---
dependency:
  name: galaxy
  enabled: false
driver:
  name: lxd
lint: |
lint: |
  set -e
  ansible-lint --exclude .github --exclude molecule -p
platforms:
  - name: instance
    source:
      alias: "${MOLECULE_DISTRO:-ubuntu/18.04}"
provisioner:
  name: ansible
verifier:
  name: ansible
