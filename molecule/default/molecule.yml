---
dependency:
  name: galaxy
  enabled: false
driver:
  name: delegated
lint: |
  set -e
  yamllint .
  ansible-lint -p .
platforms:
  - name: instance
    source:
      alias: "${MOLECULE_DISTRO:-ubuntu/18.04}"
provisioner:
  name: ansible
  connection_options:
    ansible_connection: lxd
  playbooks:
    create: "${HOME}/.ansible/roles/molecule-playbooks/lxd/create.yml"
    prepare: "${HOME}/.ansible/roles/molecule-playbooks/lxd/prepare.yml"
    destroy: "${HOME}/.ansible/roles/molecule-playbooks/lxd/destroy.yml"
verifier:
  name: ansible
